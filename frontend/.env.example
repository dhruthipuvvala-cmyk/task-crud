# Base URL of the Django REST API (include the /api/ prefix and trailing slash).
# Local dev:
VITE_API_URL=http://127.0.0.1:8000/api/
# Production example (your Render backend):
# VITE_API_URL=https://your-backend.onrender.com/api/
